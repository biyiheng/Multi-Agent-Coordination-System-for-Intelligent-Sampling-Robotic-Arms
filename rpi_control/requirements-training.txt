# =============================================================================
# 训练/监控/验证专用依赖（Round 12）
# 用于模型重训 (retrain_all / monitor_recall) 与独立验证 (test_models)。
# 运行环境仅需 numpy + pyyaml；其余为可选可复现性/测试增强。
# 安装：pip install -r requirements-training.txt
# =============================================================================
numpy>=1.24.0
pyyaml>=6.0
scipy>=1.10.0

# 训练/CI 测试工具（模型独立验证）
pytest>=7.0
pytest-asyncio>=0.21
