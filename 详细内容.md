# 智能采样机械臂系统 — 详细技术说明

---

## 🇨🇳 中文详细说明

---

## 第一部分：多方面联调机制

本项目的核心挑战在于协调 **Raspberry Pi 上位机（Python）**、**STM32 微控制器（C）**、**OpenMV 视觉模组（MicroPython）**、**六路舵机**、**Web 前端** 以及 **云端服务** 共六个异构组件的协同工作。以下从七个维度详细阐述联调机制。

---

### 1. 分层通信协议架构

系统设计了三层通信栈，确保从物理层到应用层的数据流畅通：

#### 1.1 物理层：STM32 双协议自适应串口通信

**文件**: `rpi_control/hardware/stm32_comm.py`

STM32 与 Raspberry Pi 之间通过 UART 串口通信（`/dev/serial0`，115200 baud）。系统支持 **两种协议的自适应切换**：

**自定义协议格式**：
```
#PREFIX:CMD:PARAM1,PARAM2,...!
```
- 帧头：`#`（COMMAND_PREFIX）
- 帧尾：`!`（COMMAND_TERMINATOR）
- 示例：`#ARM:MOVE:0,1500,1000!`

**YH-K32 工厂协议格式**：
```
#SSSPPPPSTTTTT!
```
- S：3 位零填充舵机编号（000-254）
- P：4 位零填充 PWM 值（0500-2500）
- T：4 位零填充时间（0000-9999）
- 多舵机：`{#000P1500T1000!#001P0900T1000!...}`
- 停止：`$DST!` 或 `$DST:x!`
- 复位：`$RST!`
- 动作组：`$DGT:start-end,count!`

**协议自动检测**（`detect_protocol()`）：
1. 发送无害的 YH-K32 测试命令（舵机 0 移动到 1500 PWM，1ms）
2. 检查响应中是否包含 "OK" 或 "YHK32"
3. 若匹配则使用 YH-K32 协议，否则回退到自定义协议
4. 最终默认使用 YH-K32（兼容大多数市售舵机控制板）

**命令映射表**：

| 操作 | 自定义协议 | YH-K32 协议 |
|------|-----------|------------|
| 移动单舵机 | `#ARM:MOVE:id,pos,time!` | `#idPposTtime!` |
| 移动全部舵机 | `#ARM:MOVE_ALL:p0,...,p5,time!` | `{#000Pp0Ttime!...#005Pp5Ttime!}` |
| 紧急停止 | `#ARM:ESTOP!` | `$DST!` |
| 软停止 | `#ARM:STOP!` | `$DST!` |
| 回原位 | `#ARM:ORIGIN!` | 全部舵机到 1500 PWM |
| 状态查询 | `#ARM:STATUS!` | `#SYS:INFO!` |
| 传感器读取 | `#SENSOR:type!` | 同上 |
| 动作组播放 | `#AG:PLAY:id!` | `$DGT:id-id,1!` |

**回显过滤机制**（`read_response()`）：
- STM32 固件会将接收到的命令原样回显（echo）
- 系统通过比较第一帧与最后发送命令来检测回显
- 若匹配，读取第二帧作为实际响应
- 若 0.5 秒内无第二帧，接受回显作为确认

**心跳机制**（`_heartbeat_loop`）：
- 每 1.0 秒发送一次心跳
- YH-K32：发送无害的舵机 0 空操作
- 自定义：发送 `SYS:INFO`
- 连续 3 次失败后进入冷却期（6 秒），然后尝试重连
- 重连尝试最多 5 次，间隔 2 秒

**线程安全**：所有串口操作均由 `asyncio.Lock` 保护。`connect()` 方法在调用 `detect_protocol()` 前显式释放锁，因为 `detect_protocol` 内部会调用 `read_response()`（也需要获取锁），避免死锁。

**平台自动检测**（`detect_rpi_port()`）：
- 读取 `/proc/device-tree/model` 判断树莓派型号
- Pi 3/4 → `/dev/serial0`
- Pi 5 → `/dev/ttyAMA0`
- Windows → `COM4`
- Linux USB-TTL → `/dev/ttyUSB0`

**仿真模式**：当 `pyserial` 不可用时，所有命令记录为 `SIM TX` 并返回 `#SIM:OK`，确保开发和测试无需物理硬件。

#### 1.2 视觉层：OpenMV 双通道通信

**文件**: `rpi_control/hardware/openmv_comm.py`

OpenMV 摄像头使用与 STM32 一致的 `#` 前缀 / `!` 后缀帧格式：

**命令格式**：`#vision:CMD:PARAM1,PARAM2,...!`

**响应解析**（`parse_response()`）支持两种格式：
1. JSON 响应（首选）：`{"status": "ok", "detections": [...]}`
2. Key:value 回退：`key1:val1,key2:val2`，自动进行数值转换（int → float → string）

**两种通信模式**：
1. **直连串口模式**：直接打开 OpenMV 的专用串口
2. **STM32 透传模式**：通过 STM32 的 UART2 桥接路由命令，命令格式为 `VISION:CMD,params`。透传模式下响应为模拟数据

**视觉命令集**：`detect_color`、`detect_apriltag`、`classify`、`inspect`、`set_threshold`、`get_image`、`version`

#### 1.3 应用层：Agent-to-Agent（A2A）协议

**文件**: `rpi_control/agents/a2a_protocol.py`

这是软件层面的智能体间通信协议，定义了类型化的消息路由机制：

**核心数据模型**：
- **`TaskContract`**：定义任务契约，包括 `input_schema`、`required_fields`、`constraints`、`expected_output`、`quality_metrics`、`exception_handlers`（异常类型到恢复动作的映射）、`max_retries`、`timeout_s`、`is_interruptible`、`checkpoint_interval_s`
- **`TaskExecution`**：运行时实例，跟踪状态、进度、重试次数、异常历史、断点数据
- **`A2AMessage`**：消息信封，包含 `message_id`、`sender`、`receiver`、`message_type`、`contract_id`、`execution_id`、`payload`、`timestamp`、`correlation_id`、`ttl_s`
- **`MESInstruction`**：制造执行系统指令，包含 `work_order_id`、`product_type`、`sampling_strategy`、`sample_count`、`quality_requirements`

**协议栈**（`A2AProtocol`）：
- 按类型注册消息处理器
- 同步请求-响应（通过 correlation_id 匹配）
- 异步即发即忘（fire-and-forget）
- 消息历史查询（按类型/发送者）
- 心跳发送

**任务状态机**（`TaskContractManager.update_state`）：
```
PENDING → ACCEPTED | FAILED
ACCEPTED → PLANNING | FAILED | ABORTED
PLANNING → EXECUTING | FAILED
EXECUTING → PAUSED | COMPLETED | FAILED | INTERRUPTED | ABORTED
PAUSED → EXECUTING | ABORTED | INTERRUPTED
INTERRUPTED → RECOVERING | ABORTED
RECOVERING → EXECUTING | FAILED
FAILED → PENDING（可重新派发）
ABORTED →（终态）
COMPLETED →（终态）
```

**异常恢复策略**（默认）：
| 异常类型 | 恢复动作链 |
|---------|-----------|
| VISION_FAILURE | visual_reinspect → retry → request_human |
| POSE_UNCERTAINTY | pose_adjust → visual_reinspect → request_human |
| GRASP_FAILURE | pose_adjust → retry → switch_strategy → skip_sample → request_human |
| COLLISION_RISK | retract_safe → emergency_stop → request_human |
| FORCE_OVERLOAD | retract_safe → emergency_stop |
| COMMUNICATION_LOSS | retry → resume_from_checkpoint → request_human |
| MATERIAL_ANOMALY | skip_sample → visual_reinspect → request_human |

**MES 集成**（`MESInterface`）：
- 产品类型到采样策略映射：`electronics→grid`、`automotive→adaptive`、`aerospace→targeted`、`consumer_goods→random`、`medical→targeted`
- `receive_instruction()` 将 MES 指令转换为 `TaskContract` 对象
- 质量指标：抓取成功率 99.5%，定位精度 0.5mm，循环时间 30s

---

### 2. 多智能体协同编排

**文件**: `rpi_control/agents/orchestrator.py`

编排器（Orchestrator）是系统的中央协调枢纽，通过 **12 状态状态机** 实现多智能体协同：

```
IDLE → PLANNING
PLANNING → APPROACHING | ABORT
APPROACHING → DETECTING | RECOVERY | ABORT
DETECTING → GRASPING | RECOVERY | ABORT
GRASPING → LIFTING | RECOVERY | ABORT
LIFTING → INSPECTING | RECOVERY | ABORT
INSPECTING → PLACING | ABORT
PLACING → EVALUATING | RECOVERY | ABORT
EVALUATING → PLANNING（循环回采样）| DONE | ABORT
DONE → IDLE
RECOVERY → PLANNING | ABORT
ABORT → IDLE
```

**智能体调度模式**：
- `_do_planning`：调用 sampling_agent + 检查 safety_agent
- `_do_approaching`：调用 motion_agent
- `_do_detecting`：调用 vision_agent
- `_do_grasping`：调用 motion_agent（使用 vision 提供的坐标）
- `_do_lifting`：调用 motion_agent
- `_do_inspecting`：调用 vision_agent + quality_agent
- `_do_placing`：调用 motion_agent（使用 quality 驱动的目标位置）
- `_do_evaluating`：调用 sampling_agent

**容错设计**：
- **故障隔离初始化**：每个智能体独立初始化，失败不影响其他智能体
- **关键智能体标识**：safety、motion 为关键智能体，失败记录 ERROR 级别
- **`_require_agent()` 门控**：状态处理器调用缺失的智能体时抛出 RuntimeError → 状态机自动转入 ABORT
- **连续错误跟踪**：连续 3 次错误后强制 ABORT
- **错误历史持久化**：保留最近 50 条错误记录用于恢复分析

**去抖安全检测**：
- 安全检测间隔最小 100ms（非每个状态转换都检测）
- ESTOP → 立即 ABORT
- DANGER → 转入 RECOVERY

**任务超时**：默认 300 秒，超时后转入 ABORT

**状态持久化**：`save_state()` / `load_state()` 支持断点续推，保存当前状态、系统状态和最近 100 条状态历史

**O(1) 状态分发**：使用预构建的 `_state_handlers` 字典，将 `OrchestratorState` 枚举值直接映射到处理器协程，避免 if-elif 链

**共享系统状态**：单个 `system_state` 字典贯穿所有状态处理器，累积以下数据：
`task_id`、`current_pose`、`workspace_bounds`、`safety_state`、`sampling_state`、`vision_result`、`quality_score`、`error`、`error_history`、`sampling_points`、`current_target`、`object_grasped`、`object_placed`、`quality_decision`、`resample_needed`、`evaluation`

---

### 3. 集成测试金字塔

系统采用四层测试金字塔，从单元到端到端逐级验证：

#### 3.1 通信协议单元测试（`test_communication.py`）

- 自包含测试双件（test doubles）：`STM32Command` 类格式化 JSON 命令
- `STM32Interface` mock 模拟连接状态、命令队列、响应队列和超时处理
- 覆盖：命令格式化、响应解析、连接生命周期、FIFO 响应排序、超时处理

#### 3.2 REST API 集成测试（`test_integration.py`）

使用 FastAPI `TestClient` 测试完整 HTTP API 表面：

| 测试类 | 端点数 | 验证内容 |
|--------|--------|---------|
| TestAPIEndpoints | 4 | root、health、docs、OpenAPI schema |
| TestArmEndpoints | 16 | 状态、位置、姿态、关节/笛卡尔/全部移动、停止、急停、回原位、夹爪开/关、工作空间 |
| TestVisionEndpoints | 8 | 状态、颜色检测、apriltag、分类、检查、阈值 |
| TestTaskEndpoints | 10 | 创建、列表、获取、启动、暂停、恢复、进度、取消、删除（含 404） |
| TestMonitorEndpoints | 6 | 系统状态、传感器、安全、日志、统计、健康 |

**端到端工作流测试**：
- `test_sampling_workflow`：create → arm check → vision check → start → cancel → delete
- `test_vision_motion_pipeline`：颜色检测驱动笛卡尔运动（检测到的 x,y 坐标直接输入 `move/cartesian`）
- `test_safety_integration`：验证急停改变安全状态是否反映在机械臂状态中

#### 3.3 循环工程管线测试（`loop_engineering/tests/test_integration.py`）

- **全管线集成**：`generate_all() → evaluate()` 流程，7 维评估器验证不同质量级别产生差异化评分
- **技能提取器**：测试轨迹模式提取、技能有效性跟踪、技能持久化
- **知识继承器**：测试版本谱系链、自动参数继承、废弃阻止、核心记忆管理、持久化
- **元优化器**：测试策略演化、收敛检测、提示模板生成、持久化
- **循环运行器**：测试完整循环运行器设置、训练回调集成、结果序列化、文件持久化
- **性能基准**：评估器 < 100ms（100 任务）、上下文管理器 < 200ms（500 更新）、技能提取器 < 500ms（50 轨迹 × 10 动作）

#### 3.4 综合测试运行器（`run_all_tests.py`）

- 四大测试套件：`blackbox`（API 测试）、`whitebox`（核心逻辑）、`performance`（基准测试）、`security`（漏洞扫描）
- 子进程执行：每个套件通过 `pytest` 子进程运行，300 秒超时
- 结果解析：基于正则表达式解析 pytest 输出
- 报告生成：JSON 报告，包含摘要统计、每套件细分和自动生成的建议
- CLI 标志：`--quick`（跳过性能测试）、`--verbose`、`--json`、`--report`、`--suites`
- 建议引擎：根据失败套件生成针对性建议

---

### 4. Docker 容器化部署

**文件**: `rpi_control/Dockerfile`、`rpi_control/docker-compose.yml`

#### 4.1 多阶段 Docker 构建

- **构建阶段**：Python 3.11-slim-bookworm，安装 gcc/g++/make、atlas、openblas，pip 安装依赖到 `/install`
- **生产阶段**：仅从构建阶段复制已安装包，添加运行时依赖（libatlas、libopenblas、libgomp1、curl、i2c-tools、libgpiod2），创建非 root `sampling` 用户，复制应用代码到 `/app/rpi_control/`
- 暴露端口 8000（REST API）和 8001（WebSocket）
- 健康检查：`curl -f http://localhost:8000/api/health`
- 入口：`python -m rpi_control.main`
- 支持多架构：`linux/amd64` 和 `linux/arm64`（通过 `docker buildx`）

#### 4.2 Docker Compose 四服务编排

| 服务 | 激活条件 | 功能 |
|------|---------|------|
| rpi-control | 始终启动 | REST API + WebSocket，挂载 `/dev/serial0`、`/dev/gpiomem` 等硬件设备，资源限制 1.5 CPU / 512MB 内存 |
| cloud-sync | profile `cloud` | 每 60 秒异步同步循环，依赖 rpi-control 健康 |
| model-trainer | profile `training` | 运行 `python -m training.run_training --rounds 1`，一次性执行 |
| loop-trainer | profile `loop`/`all` | 运行 `--loop --loop-iterations 10 --rounds 1`，一次性执行 |

所有服务共享 `sampling-arm-network` 桥接网络。

---

### 5. WebSocket 实时遥测

**文件**: `rpi_control/web/websocket/telemetry.py`、`rpi_control/web/websocket/handler.py`

#### 5.1 WebSocket 连接管理器

- 最大 100 个并发客户端
- UUID 客户端标识
- `asyncio.Lock` 线程安全
- `broadcast()` 向所有客户端发送 JSON，自动移除断线客户端
- `send_to_client()` 定向发送

#### 5.2 生产级遥测流

10 Hz 推送频率（可配置），从四个数据源采集：

| 数据源 | 内容 |
|--------|------|
| 机械臂状态 | 关节位置、速度、末端执行器位姿、is_moving、is_homed、夹爪状态/力 |
| 安全智能体 | 急停标志、安全级别、警告、关节限位状态、工作空间状态、碰撞风险 |
| 传感器数据 | 温度、湿度、电压、电流、距离 |
| 系统状态 | CPU 使用率、内存使用率、运行时间、连接客户端数 |

每条遥测消息包含序列号和时间戳。支持事件驱动消息：`send_status_change()` 用于状态变更，`send_error()` 用于错误广播。

---

### 6. 硬件仿真与真实双模式

系统在所有硬件接口层实现了 **仿真/真实双模式**，这是联调的关键设计：

- **STM32**：当 `pyserial` 不可用时自动进入仿真模式，所有命令记录为 `SIM TX` 并返回 `#SIM:OK`
- **OpenMV**：连接失败时设置 `_connected = True` 进入仿真模式
- **测试夹具**：`conftest.py` 中的 `_disable_hardware` autouse fixture 通过 monkeypatch 强制 `HAS_PYSERIAL = False`
- **硬件测试标记**：`@pytest.mark.hardware` 标记的测试默认跳过，仅当 `--hardware` 标志提供时运行

这使得开发者可以在没有物理硬件的情况下完成 90% 以上的开发和测试工作。

---

### 7. 故障隔离与容错

#### 7.1 智能体故障隔离

```python
agent_definitions = [
    ("sampling", SamplingAgent, ..., False),   # 非关键
    ("vision", VisionAgent, ..., False),        # 非关键
    ("motion", MotionAgent, ..., True),         # 关键
    ("quality", QualityAgent, ..., False),      # 非关键
    ("safety", SafetyAgent, ..., True),         # 关键
]
```

每个智能体独立初始化，关键智能体失败记录 ERROR，非关键智能体失败记录 WARNING。

#### 7.2 错误通知器

**文件**: `rpi_control/utils/error_handler.py`

- `ErrorNotifier` 单例收集运行时错误
- `asyncio.Lock` 保护共享状态
- `collections.deque(maxlen=100)` 自动截断错误历史（O(1) 操作）
- 重试装饰器：支持同步/异步函数，可配置最大重试次数和退避策略
- `CancelledError` 显式传播：防止取消信号被吞没

#### 7.3 配置加载器

**文件**: `rpi_control/utils/config_loader.py`

- 支持 YAML 配置 + 环境变量覆盖（`${ENV_VAR}` 语法）
- 深度合并（deep merge）
- 布尔值和数值自动解析

---

### 8. 固件验证与系统诊断

**文件**: `verify_firmware.py`、`rpi_control/scripts/verify_system.py`

#### 8.1 STM32 固件验证

- 自动扫描可用串口
- 发送 `#SYS:INFO!` 探测固件
- 13 项伺服协议测试：单舵机移动、多舵机移动、全部停止、单舵机停止、复位、动作组、系统信息、伺服状态、传感器读取、紧急停止、回原位、最小/最大 PWM 位置
- 每项测试检查响应与预期模式匹配
- 生成 JSON 报告（含通过/失败详情和耗时）

#### 8.2 系统综合验证

11 个验证部分：
1. 智能体导入验证（7 个模块）
2. 编排器初始化
3. 模型文件（pickle 可加载性、元数据）
4. 训练报告（JSON 解析、改进指标）
5. Docker/RPi 配置（文件存在性、RPi 关键词检测）
6. 配置文件（YAML 解析）
7. 训练数据质量（样本计数、完整性、边界情况数据集）
8. 硬件接口（STM32、舵机、OpenMV、串口）
9. 性能基准（FK/IK 耗时、神经网络前向传播）
10. RPi 健康（温度、降频、时钟、电压、内存、UART/I2C/GPIO）
11. 交叉验证准备（最小样本计数）

---

## 第二部分：核心算法详细说明

---

### 1. 运动学算法

**文件**: `rpi_control/motion/kinematics.py`

#### 1.1 DH 参数表

机械臂采用 **标准 DH 约定**（非 Modified DH）。默认参数以 `[a, alpha, d, theta_offset]` 元组存储：

| 关节 | a (mm) | α (deg) | d (mm) | θ_offset (deg) | 描述 |
|------|--------|---------|--------|----------------|------|
| 0 | 0 | 90 | 85 | 0 | 底座旋转 |
| 1 | 120 | 0 | 0 | -90 | 肩关节 |
| 2 | 110 | 0 | 0 | 0 | 肘关节 |
| 3 | 0 | 90 | 0 | 0 | 腕俯仰 |
| 4 | 0 | -90 | 95 | 0 | 腕横滚 |
| 5 | 0 | 0 | 65 | 0 | 夹爪 |

#### 1.2 正运动学（FK）— 齐次变换链

每个 DH 变换由 `transformation_matrix(dh, theta)` 计算：

```
有效角度: θ_eff = θ + dh.θ_offset
变换矩阵: T = Rot_z(θ_eff) · Trans_z(d) · Trans_x(a) · Rot_x(α)
```

4×4 齐次矩阵：

```
T = [
  [cos(θ_eff),  -sin(θ_eff)·cos(α),   sin(θ_eff)·sin(α),  a·cos(θ_eff)],
  [sin(θ_eff),   cos(θ_eff)·cos(α),  -cos(θ_eff)·sin(α),  a·sin(θ_eff)],
  [0,            sin(α),               cos(α),              d           ],
  [0,            0,                    0,                   1           ]
]
```

`forward_kinematics(joint_angles)` 函数迭代相乘：`T = I @ T₀ @ T₁ @ ... @ T₅`，返回最终末端执行器位姿和 6 个中间累积变换（用于雅可比矩阵计算）。

**欧拉角提取**（ZYX 约定）：
- 非奇异（sy = √(r₀₀² + r₁₀²) > 1e-6）：`roll = atan2(R₂₁, R₂₂)`，`pitch = atan2(-R₂₀, sy)`，`yaw = atan2(R₁₀, R₀₀)`
- 奇异（pitch ≈ ±90°，万向节锁）：`roll = atan2(-R₁₂, R₁₁)`，`pitch = atan2(-R₂₀, sy)`，`yaw = 0.0`

#### 1.3 逆运动学（IK）— Pieper 解耦方法

**算法流程：**

**步骤 1 — 欧拉角转旋转矩阵**：`R_desired = R_z(yaw) · R_y(pitch) · R_x(roll)`

**步骤 2 — 腕心位置**：从目标位置减去末端执行器 Z 轴偏移
```
腕心 = [x, y, z] - d₆ · R_desired[:, 2]
```
其中 d₆ = dh_params[5].d = 65 mm

**步骤 3 — 位置 IK（关节 0-2）**：
- **θ₁**：`atan2(y, x)` 和 `atan2(y, x) + π`（两个对跖方向），通过 `atan2(sin, cos)` 归一化到 [-π, π]
- 对每个有效 θ₁：
  - 计算 `r = √(x² + y²)`，`z' = z - d₁`
  - **θ₃**（余弦定理）：`cos(θ₃) = (r² + z'² - a₁² - a₂²) / (2·a₁·a₂)`，钳制到 [-1, 1]，两个解：`θ₃ = acos(cosθ₃)`（肘上）和 `θ₃ = -acos(cosθ₃)`（肘下）
  - **θ₂**：`θ₂ = atan2(z', r) - atan2(k₂, k₁)`，其中 `k₁ = a₁ + a₂·cos(θ₃)`，`k₂ = a₂·sin(θ₃)`

**步骤 4 — 姿态 IK（关节 3-5）**：
- 计算 `R₃₆ = R₀₃ᵀ · R_desired`
- **θ₅**：`acos(R₃₆[2,2])`，钳制到 [-1, 1]，两个解：`θ₅` 和 `-θ₅`（腕翻转）
- 非奇异（|sin(θ₅)| ≥ 1e-6）：`θ₄ = atan2(R₃₆[1,2]/sin(θ₅), R₃₆[0,2]/sin(θ₅))`，`θ₆ = atan2(R₃₆[2,1]/sin(θ₅), -R₃₆[2,0]/sin(θ₅))`
- 奇异（|sin(θ₅)| < 1e-6）：`θ₄ = 0.0`，`θ₆ = atan2(R₃₆[1,0], R₃₆[0,0])`（万向节锁处理）

**步骤 5 — 过滤排序**：最多 2×2×2 = 8 个解，经关节限位验证后按最小关节位移欧氏距离排序

**复杂度**：O(1) 每个解

#### 1.4 雅可比矩阵 — 解析几何雅可比

对每个关节 i：
- 若 i == 0：`z_i = [0,0,1]`，`p_i = [0,0,0]`
- 若 i > 0：`z_i = T_prev[:3, 2]`，`p_i = T_prev[:3, 3]`（从关节 i-1 的累积变换提取）
- 线速度列：`J_v[:, i] = z_i × (p_end - p_i)`（叉积）
- 角速度列：`J_ω[:, i] = z_i`

标准几何雅可比：`v = J · q̇`，其中 `v = [vx, vy, vz, ωx, ωy, ωz]`

#### 1.5 PWM 转换

```
常量: PWM_MIN = 500, PWM_MAX = 2500

角度 → PWM:
  center = 1500
  scale = 2000/π  (PWM 单位/弧度)
  pwm = round(1500 + angle × scale)，钳制到 [500, 2500]

PWM → 角度:
  scale = π/2000  (弧度/PWM 单位)
  angle = (pwm - 1500) × scale
```

---

### 2. 轨迹规划算法

**文件**: `rpi_control/motion/trajectory.py`

#### 2.1 五次多项式插值

**函数**: `plan_joint_path(start_joints, end_joints, duration, dt=0.02)`

**公式**：
```
s = t / duration  (归一化时间，s ∈ [0, 1])
混合函数: blend(s) = 10s³ - 15s⁴ + 6s⁵
```

**边界条件**：
- 位置：blend(0) = 0，blend(1) = 1
- 速度：blend'(0) = 0，blend'(1) = 0（端点零速度）
- 加速度：blend''(0) = 0，blend''(1) = 0（端点零加速度）

**每个关节 j 的位置**：
```
θⱼ(t) = startⱼ + blend(s) × (endⱼ - startⱼ)
θ̇ⱼ(t) = blend'(s) / duration × (endⱼ - startⱼ)
```

**复杂度**：O(n)，n = duration/dt。多项式保证 C² 连续性。

#### 2.2 S 形速度曲线

**函数**: `s_curve_profile(distance, max_vel, max_accel, max_jerk, dt=0.01)`

**七段式曲线**：
| 阶段 | 时间范围 | 加加速度 | 加速度 | 速度 |
|------|---------|---------|--------|------|
| 1 | t < t_j | +J_max | 线性增加 | 二次增加 |
| 2 | t_j ≤ t < t_a - t_j | 0 | 恒定 | 线性增加 |
| 3 | t_a - t_j ≤ t < t_a | -J_max | 线性减少至 0 | 二次增加至 V_max |
| 4 | t_a ≤ t < t_a + t_v | 0 | 0 | 恒定 |
| 5 | t_a + t_v ≤ t < t_a + t_v + t_j | -J_max | 线性增加 | 二次减少 |
| 6 | t_a + t_v + t_j ≤ t < t_a + t_v + t_a - t_j | 0 | 恒定 | 线性减少 |
| 7 | t_a + t_v + t_a - t_j ≤ t < t_total | +J_max | 线性减少至 0 | 二次减少至 0 |

**阶段时间计算**：
- `t_j = max_accel / max_jerk`
- 若 `max_vel < max_accel² / max_jerk`：三角加速度（无恒定加速度段），`t_j = √(max_vel / max_jerk)`，`t_a = 2t_j`
- 否则：梯形加速度，`t_a = max_vel / max_accel + t_j`
- 加速距离：`s_accel = J_max·t_j³/6 + A_max·(t_a - 2t_j)·(t_a + t_j)/2 + J_max·t_j³/3`
- 若 `2·s_accel > distance`：三角速度（无恒定速度段），重新计算 `t_j = (distance/(2·J_max))^(1/3)`，`t_a = 2t_j`，`t_v = 0`
- 否则：`t_v = (distance - 2·s_accel) / max_vel`

**数值积分**（欧拉法）：
```
accel += j × dt     （钳制到 [-A_max, A_max]）
vel   += accel × dt （钳制到 [0, V_max]）
pos   += vel × dt   （钳制到 [0, distance]）
```

**复杂度**：O(t_total / dt)

#### 2.3 梯形速度曲线

**函数**: `trapezoidal_profile(distance, max_vel, max_accel, dt=0.01)`

**三段式**：
1. 加速：`vel = A_max · t`，`pos = ½A_max · t²`
2. 恒定速度：`vel = V_max`，`pos = s_accel + V_max · (t - t_accel)`
3. 减速：`vel = V_max - A_max · t_dec`，`pos = s_accel + V_max · t_const + V_max · t_dec - ½A_max · t_dec²`

**与 S 曲线的关键区别**：使用解析闭式公式计算位置（非数值积分），加加速度在阶段转换处为无穷大（加速度不连续）。

#### 2.4 路径平滑

**函数**: `smooth_path(raw_path, smoothing_factor=0.5, iterations=3)`

**迭代移动平均滤波**（离散拉普拉斯平滑）：
```
平滑后[i] = (1 - α) · p[i] + (α/2) · p[i-1] + (α/2) · p[i+1]
```
首尾点保持不变。α = smoothing_factor ∈ [0, 1]。

**复杂度**：O(iterations × n)

#### 2.5 笛卡尔线性路径

**函数**: `plan_linear_path(start_pose, end_pose, steps=50)`

- 位置：线性插值 `pos = start_pos + t × (end_pos - start_pos)`
- 姿态：欧拉角插值，最短路径包裹 `wrapped_diff = atan2(sin(diff), cos(diff))`

---

### 3. 碰撞检测算法

**文件**: `rpi_control/motion/collision.py`

#### 3.1 AABB 碰撞检测

**类 `AABB`**：存储 `min_point` 和 `max_point`。

**相交测试**（O(1)）：
```
所有轴满足: min_point ≤ other.max_point 且 max_point ≥ other.min_point
```

#### 3.2 自碰撞检测

**连杆建模**：机械臂建模为 5 个圆柱形连杆段（半径 = 25.0 mm），相邻连杆对排除检查。

**算法**（`check_self_collision`）：
1. 计算 FK 获取所有 6 个关节在 3D 空间中的位置
2. 对每个非相邻连杆对 (i, j)：
   - 计算 `_segment_to_segment_distance(p1, p2, q1, q2)` — 两线段间最小距离
   - 阈值 = `radius_i + radius_j + safety_margin`（默认：25 + 25 + 10 = 60 mm）
   - 若距离 < 阈值：检测到碰撞，`穿透深度 = 阈值 - 距离`

**线段到线段距离算法**（`_segment_to_segment_distance`）：
1. 计算 `d1 = p2 - p1`，`d2 = q2 - q1`，`r = q1 - p1`
2. 计算 `a = |d1|²`，`e = |d2|²`，`f = d2·r`，`c = d1·r`，`b = d1·d2`
3. 退化情况处理（点、平行线）
4. 一般情况：`s = clamp((b·f - c·e) / (a·e - b²), 0, 1)`，`t = clamp((a·f - b·c) / (a·e - b²), 0, 1)`
5. 距离 = `|(p1 + s·d1) - (q1 + t·d2)|`

**复杂度**：O(C(5,2) - 5) = O(5) = O(1)

#### 3.3 环境碰撞检测

**两阶段算法**（`check_environment_collision`）：
1. **粗检测（Broad Phase）**：AABB 测试（O(1) 过滤）
2. **精检测（Narrow Phase）**：点到线段距离（`_point_to_segment_distance`），阈值 = `link.radius + obstacle.radius + safety_margin`

**复杂度**：O(L × O)，L = 连杆数 (5)，O = 障碍物数。AABB 预过滤消除大部分检查。

#### 3.4 安全撤退路径生成

**函数**: `get_safe_retreat_path(current_pos, home_pos=None, obstacles=None, step_size=0.05)`

1. 生成 `num_steps = 1/step_size` 个插值关节配置
2. 每步检查环境碰撞
3. **回退策略**：若检测到碰撞，尝试抬高肩关节（关节 1）0.2 弧度。若仍然受阻，走原始路径（尽力而为）

---

### 4. 工作空间分析算法

**文件**: `rpi_control/motion/workspace.py`

#### 4.1 蒙特卡洛工作空间边界

**函数**: `compute_workspace_boundary(num_samples=10000, joint_limits=None)`

**算法**：
1. 循环 `num_samples` 次：在每个关节限位内均匀随机生成关节角度，计算 FK 获取末端执行器位置，追加到点列表
2. 返回 N×3 可达位置点云

**复杂度**：O(num_samples × 6)

#### 4.2 可达性检查

**函数**: `is_point_reachable(x, y, z)`

创建零姿态目标位姿 `[x, y, z, 0, 0, 0]` 并调用 IK。返回是否存在至少一个有效解。**复杂度**：O(1)。

#### 4.3 灵巧度分析

**函数**: `compute_dexterity(point)` — Yoshikawa 可操作度度量：

```
w = √(det(J · Jᵀ))
```

- 更高的 w = 更好的灵巧度（远离奇异点）
- w = 0 在奇异点或不可达点

#### 4.4 采样网格生成

**矩形模式**：Z → Y → X 三层嵌套循环，步长 = spacing。仅包含可达点。

**六边形模式**：行偏移交错网格，`hex_spacing_y = spacing × √3/2`，奇数行 X 偏移 `spacing/2`。

**复杂度**：O((x_range/spacing) × (y_range/spacing) × (z_range/spacing) × IK_cost)

#### 4.5 TSP 采样顺序优化

**函数**: `optimize_sampling_order(points, start_pos=None)` — 贪心最近邻启发式：

1. 从 start_pos 开始
2. 重复找最近未访问点并移动
3. 直到所有点被访问

**复杂度**：O(n²)

---

### 5. 力位混合控制算法

**文件**: `rpi_control/motion/force_control.py`

#### 5.1 阻抗控制

**质量-弹簧-阻尼模型**：
```
M · ẍ + D · (ẋ - ẋ_d) + K · (x - x_d) = F_ext
```
其中 M = 惯性，D = 阻尼，K = 刚度（均为对角 6×6 矩阵）。

**实现**（求解加速度）：
```
位置误差 = x - x_d
加速度 = (F_ext - D · ẋ - K · 位置误差) / (M + 1e-10)
```

**位置修正**（二阶积分）：
```
Δx = ẋ · dt + ½ · 加速度 · dt²
```
其中 dt = 0.001s（1ms 控制周期）。

**默认参数**：
- 刚度（平移）：500 N/m，刚度（旋转）：50 Nm/rad
- 阻尼（平移）：50 Ns/m，阻尼（旋转）：10 Nms/rad
- 惯性（平移）：1 kg，惯性（旋转）：0.1 kg·m²

**方向刚度**：`K_effective = K_base - 0.8 × base_stiffness × (d_norm · d_normᵀ)`，在指定方向降低 80% 刚度。

#### 5.2 导纳控制

**公式**：
```
M_d · ẍ + D_d · ẋ = F_ext - F_d
```
无刚度项（无弹簧），系统作为阻尼质量响应力误差。

**实现**：
```
力误差 = F_measured - F_desired
加速度 = (力误差 - D · ẋ) / (M + 1e-10)
Δx = ẋ · dt + ½ · 加速度 · dt²
```

**关键区别**：导纳控制从力输入计算位置修正，适用于高刚度环境。

#### 5.3 力位混合控制

**公式**：
```
τ = S · τ_pos + (I - S) · τ_force
```
其中 S 是 6×6 对角选择矩阵。`S[i,i] = 1` 表示轴 i 上的位置控制；`S[i,i] = 0` 表示力控制。

实现使用 `ImpedanceController` 处理位置控制轴，`AdmittanceController` 处理力控制轴。

#### 5.4 柔顺抓取状态机

**6 状态**：APPROACH → CONTACT → GRASPING → GRASPED → RELEASING → RELEASED（+ FAILED 从 GRASPED 滑移时）

**状态转换**：
| 转换 | 条件 |
|------|------|
| APPROACH → CONTACT | 力幅值 > 1.5N |
| CONTACT → GRASPING | 即时转换 |
| GRASPING → GRASPED | 力幅值 > 3.0N |
| GRASPED → FAILED | 滑移检测触发 |
| RELEASING → RELEASED | 力幅值 < 0.1N 且夹爪位置 > 10.0mm |

#### 5.5 滑移检测算法

**双条件检测**：
```
力下降 = mean(较早10个样本) - mean(最近10个样本)
标准差增加 = std(最近10) - std(较早10)
滑移 = (力下降 > 0.3N) 且 (标准差增加 > 0.1)
```

**原理**：物体滑移时，夹持力下降（均值减小）且变得不稳定（方差增加）。

#### 5.6 轴孔装配搜索

**函数**: `spiral_search_path` — 阿基米德螺旋线：

```
总角度 = num_turns × 2π
r(t) = radius_max × t / 总角度  （线性半径增长）
x = center_x + r × cos(t)
y = center_y + r × sin(t)
```

参数：radius_max = 5.0mm，pitch = 0.5mm，num_turns = 3。从中心向外扩展，覆盖 5mm 半径区域。

#### 5.7 末端执行器标定 — SVD 刚性变换

1. 计算测量点集和名义点集的质心
2. 构造交叉协方差矩阵 H = (measured - centroid_m)ᵀ · (nominal - centroid_n)
3. SVD：H = U · S · Vᵀ
4. 旋转：R = V · Uᵀ（若 det(R) < 0 则修正符号）
5. 平移：t = centroid_n - R · centroid_m
6. 返回 4×4 齐次变换矩阵

需要至少 3 个点对。

---

### 6. 硬实时安全控制算法

**文件**: `rpi_control/safety/realtime_safety.py`

#### 6.1 控制周期

- 控制循环：**1 ms（1000 Hz）**
- 故障响应时限：**< 10 ms**
- 安全状态历史：1000 条

#### 6.2 安全状态机

```
NORMAL ↔ REDUCED_SPEED ↔ PROTECTIVE_STOP ↔ SAFEGUARD_STOP ↔ EMERGENCY_STOP
                                                                     ↕
                                                                  FAULT
                                                                     ↕
                                                                 RECOVERY
```

#### 6.3 每周期 10 步安全检查

每个控制周期（1ms）执行：
1. 更新关节状态（位置、速度、力矩）
2. 更新 TCP 速度
3. 检查关节限位（位置超限 → JOINT_LIMIT_VIOLATION）
4. 检查速度限制（关节速度 > 5.0 rad/s → SPEED_LIMIT_EXCEEDED；TCP 速度 > 1000 mm/s → 严重违规）
5. 碰撞检测（基于距离：< 300mm 警告，< 100mm 保护停止；基于力矩：总力矩 > 10 Nm 阈值）
6. 力矩限制检查（单关节 > 50 Nm → TORQUE_OVERLOAD）
7. ISO/TS 15066 力限制（接触力 > 65N（脸部限值）→ FORCE_LIMIT_EXCEEDED）
8. 通信检查（心跳超时 > 100ms → COMMUNICATION_TIMEOUT）
9. 时钟同步检查（漂移 > 100 ppm → CLOCK_DRIFT）
10. 更新安全状态（根据最严重事件决定状态转换）

#### 6.4 ISO/TS 15066:2016 合规

**协作力限值**（人体部位）：

| 部位 | 力 (N) | 压力 (N/cm²) |
|------|--------|-------------|
| 头骨/前额 | 175 | 130 |
| 面部 | 65 | 65 |
| 颈部 | 150 | 140 |
| 背部/肩部 | 210 | 160 |
| 胸部 | 140 | 120 |
| 腹部 | 110 | 140 |
| 上臂/肘部 | 150 | 190 |
| 前臂/腕部 | 150 | 190 |
| 手/手指 | 140 | 240 |
| 大腿/膝盖 | 220 | 220 |
| 小腿 | 180 | 220 |

**安全速度限制**：
- 减速模式：250 mm/s
- 安全监控速度：2000 mm/s
- TCP 最大速度：1000 mm/s
- 关节最大速度：5.0 rad/s

**安全距离**：
- 最小分离距离：200 mm
- 保护停止距离：100 mm
- 减速区域：500 mm
- 碰撞预警距离：300 mm

#### 6.5 网络冗余与时钟同步

- 主备双网络状态监控
- 双网同时丢失 → 触发严重通信超时事件
- 时钟同步支持 PTP（IEEE 1588）和 NTP
- 漂移检测：超过 100 ppm 触发警告

#### 6.6 安全回原位

使用 Smoothstep 插值（`s = 3t² - 2t³`），50 步，每步最大 0.05 rad，确保 C¹ 连续的回原位路径。

---

### 7. 循环工程（Loop Engineering）框架

**文件**: `rpi_control/loop_engineering/`、`rpi_control/config/settings.yaml`

#### 7.1 框架概述

循环工程是一个 **自动化闭环优化方法论**：提出变更 → 训练/执行 → 评估 → 保留好的改进。通过低成本的持续试错，在自动化闭环中逼近最优解。

#### 7.2 七大组件

| 组件 | 功能 | 关键参数 |
|------|------|---------|
| **Profiler（性能分析器）** | 实时检测慢操作，识别性能瓶颈 | 慢操作阈值 100ms，Top N 报告 |
| **Interaction Tracker（交互追踪器）** | 检测冗余调用，监控上下文膨胀 | 冗余阈值 3 次，上下文警告 20 条消息 |
| **Evaluator（七维评估器）** | 量化系统在 7 个维度的表现 | 延迟 15%、可靠性 20%、质量 15%、效率 15%、鲁棒性 15%、上下文健康度 10%、技能复用性 10% |
| **Context Manager（上下文管理器）** | 自动压缩过长上下文，防止状态衰减 | 最大历史 100 条，压缩阈值 50 条消息 |
| **Skill Extractor（技能提取器）** | 挖掘高频复用模式，提炼为可复用技能 | 最小复用 2 次，相似度阈值 0.7 |
| **Knowledge Inheritor（知识继承器）** | 跨版本知识迁移，淘汰失效知识 | 核心记忆保留 80%，衰减阈值 0.3 |
| **Meta-Optimizer（元优化器）** | 演化策略优化，持续逼近最优配置 | 最小改进阈值 0.05，策略历史 50 条 |

#### 7.3 闭环运行器（Loop Runner）

- 最大循环迭代次数：10
- 收敛阈值：连续改进 < 0.01
- 收敛所需连续次数：3
- 循环间最小间隔：1.0 秒
- 回调：`on_iteration`、`on_convergence`

---

## 🇬🇧 English Detailed Explanation

---

## Part 1: Multi-Faceted Joint Debugging Mechanism

The core challenge of this project is coordinating six heterogeneous components: **Raspberry Pi host (Python)**, **STM32 microcontroller (C)**, **OpenMV vision module (MicroPython)**, **six servo motors**, **web frontend**, and **cloud services**. The joint debugging mechanism is detailed across seven dimensions.

---

### 1. Layered Communication Protocol Architecture

The system implements a three-layer communication stack ensuring smooth data flow from physical to application layer.

#### 1.1 Physical Layer: STM32 Dual-Protocol Adaptive Serial Communication

**File**: `rpi_control/hardware/stm32_comm.py`

STM32 communicates with Raspberry Pi via UART serial (`/dev/serial0`, 115200 baud). The system supports adaptive switching between two protocols:

**Custom Protocol Format**:
```
#PREFIX:CMD:PARAM1,PARAM2,...!
```
- Frame header: `#` (COMMAND_PREFIX)
- Frame terminator: `!` (COMMAND_TERMINATOR)
- Example: `#ARM:MOVE:0,1500,1000!`

**YH-K32 Factory Protocol Format**:
```
#SSSPPPPSTTTTT!
```
- S: 3-digit zero-padded servo index (000-254)
- P: 4-digit zero-padded PWM value (0500-2500)
- T: 4-digit zero-padded time (0000-9999)
- Multi-servo: `{#000P1500T1000!#001P0900T1000!...}`
- Stop: `$DST!` or `$DST:x!`
- Reset: `$RST!`
- Action groups: `$DGT:start-end,count!`

**Auto-Detection** (`detect_protocol()`):
1. Sends a harmless YH-K32 test command (servo 0 move to 1500 PWM, 1ms)
2. Checks response for "OK" or "YHK32"
3. Matches → YH-K32 protocol; otherwise falls back to custom protocol
4. Defaults to YH-K32 (compatible with most off-the-shelf servo controllers)

**Command Mapping**:

| Operation | Custom Protocol | YH-K32 Protocol |
|-----------|----------------|-----------------|
| Move single servo | `#ARM:MOVE:id,pos,time!` | `#idPposTtime!` |
| Move all servos | `#ARM:MOVE_ALL:p0,...,p5,time!` | `{#000Pp0Ttime!...#005Pp5Ttime!}` |
| Emergency stop | `#ARM:ESTOP!` | `$DST!` |
| Soft stop | `#ARM:STOP!` | `$DST!` |
| Return to origin | `#ARM:ORIGIN!` | All servos to 1500 PWM |
| Status query | `#ARM:STATUS!` | `#SYS:INFO!` |
| Sensor read | `#SENSOR:type!` | Same |
| Action group play | `#AG:PLAY:id!` | `$DGT:id-id,1!` |

**Echo Filtering** (`read_response()`):
- STM32 firmware echoes received commands verbatim
- System detects echo by comparing first received frame to last sent command
- If matched, reads a second frame for actual response
- If no second frame within 0.5s, accepts echo as acknowledgment

**Heartbeat** (`_heartbeat_loop`):
- Sends ping every 1.0s
- YH-K32: harmless servo 0 no-op; Custom: `SYS:INFO`
- After 3 consecutive failures: cooldown period (6s), then reconnection attempts
- Reconnection: up to 5 attempts, 2s delay between

**Thread Safety**: All serial operations protected by `asyncio.Lock`. `connect()` releases lock before `detect_protocol()` to avoid deadlock.

**Platform Auto-Detection** (`detect_rpi_port()`):
- Reads `/proc/device-tree/model` to determine Pi model
- Pi 3/4 → `/dev/serial0`; Pi 5 → `/dev/ttyAMA0`; Windows → `COM4`; Linux USB-TTL → `/dev/ttyUSB0`

**Simulation Mode**: When `pyserial` is unavailable, all commands logged as `SIM TX` and return `#SIM:OK`.

#### 1.2 Vision Layer: OpenMV Dual-Channel Communication

**File**: `rpi_control/hardware/openmv_comm.py`

Uses the same `#` prefix / `!` terminator framing as STM32.

**Command Format**: `#vision:CMD:PARAM1,PARAM2,...!`

**Response Parsing**: JSON (preferred) and key:value fallback with automatic numeric conversion.

**Two Communication Modes**:
1. **Direct Serial**: Dedicated serial port to OpenMV camera
2. **STM32 Passthrough**: Routes through STM32's UART2 bridge; commands sent as `VISION:CMD,params`

**Vision Commands**: `detect_color`, `detect_apriltag`, `classify`, `inspect`, `set_threshold`, `get_image`, `version`

#### 1.3 Application Layer: Agent-to-Agent (A2A) Protocol

**File**: `rpi_control/agents/a2a_protocol.py`

Software-level typed message routing between agents:

**Core Data Models**:
- **`TaskContract`**: Defines input/output/exception boundaries with recovery strategies
- **`TaskExecution`**: Runtime instance tracking state, progress, retries, exception history, checkpoints
- **`A2AMessage`**: Envelope with message_id, sender, receiver, type, contract_id, execution_id, payload, timestamp, correlation_id, ttl
- **`MESInstruction`**: Manufacturing Execution System integration

**Task State Machine (10 states)**:
```
PENDING → ACCEPTED | FAILED
ACCEPTED → PLANNING | FAILED | ABORTED
PLANNING → EXECUTING | FAILED
EXECUTING → PAUSED | COMPLETED | FAILED | INTERRUPTED | ABORTED
PAUSED → EXECUTING | ABORTED | INTERRUPTED
INTERRUPTED → RECOVERING | ABORTED
RECOVERING → EXECUTING | FAILED
FAILED → PENDING (re-dispatchable)
ABORTED → (terminal)
COMPLETED → (terminal)
```

**Exception Recovery Strategies** (default):
| Exception Type | Recovery Action Chain |
|---------------|----------------------|
| VISION_FAILURE | visual_reinspect → retry → request_human |
| POSE_UNCERTAINTY | pose_adjust → visual_reinspect → request_human |
| GRASP_FAILURE | pose_adjust → retry → switch_strategy → skip_sample → request_human |
| COLLISION_RISK | retract_safe → emergency_stop → request_human |
| FORCE_OVERLOAD | retract_safe → emergency_stop |
| COMMUNICATION_LOSS | retry → resume_from_checkpoint → request_human |

**MES Integration**: Product-type to sampling-strategy mapping with quality metrics (grasp success rate 99.5%, position accuracy 0.5mm, cycle time 30s).

---

### 2. Multi-Agent Collaborative Orchestration

**File**: `rpi_control/agents/orchestrator.py`

The Orchestrator is the central coordination hub with a 12-state state machine:

```
IDLE → PLANNING → APPROACHING → DETECTING → GRASPING → LIFTING
→ INSPECTING → PLACING → EVALUATING → DONE/PLANNING(loop)
```

With fault paths: RECOVERY and ABORT from most states.

**Agent Dispatch Pattern**:
- `_do_planning`: sampling_agent + safety_agent check
- `_do_approaching`: motion_agent
- `_do_detecting`: vision_agent
- `_do_grasping`: motion_agent (with vision-provided coordinates)
- `_do_lifting`: motion_agent
- `_do_inspecting`: vision_agent + quality_agent
- `_do_placing`: motion_agent (quality-driven destination)
- `_do_evaluating`: sampling_agent

**Fault Tolerance**:
- Fault-isolated agent initialization
- Critical agents (safety, motion) log at ERROR, non-critical at WARNING
- `_require_agent()` gates: missing agent → RuntimeError → ABORT
- Consecutive error tracking: 3 errors → forced ABORT
- Error history persisted (last 50 entries) for recovery analysis

**Debounced Safety Checks**: Minimum 100ms between checks. ESTOP → immediate ABORT. DANGER → RECOVERY.

**Task Timeout**: Default 300s, on timeout → ABORT.

**State Persistence**: `save_state()` / `load_state()` for checkpointing. Saves current state, system_state, and last 100 state history entries.

**O(1) State Dispatch**: Pre-built `_state_handlers` dict mapping `OrchestratorState` enum directly to handler coroutines.

**Shared System State**: Single `system_state` dict accumulated through all state handlers.

---

### 3. Integration Test Pyramid

Four-layer test pyramid from unit to end-to-end:

#### 3.1 Communication Protocol Unit Tests (`test_communication.py`)
- Self-contained test doubles: `STM32Command`, `STM32Interface` mock
- Covers: command formatting, response parsing, connection lifecycle, FIFO ordering, timeout handling

#### 3.2 REST API Integration Tests (`test_integration.py`)
Uses FastAPI `TestClient` with 5 test classes covering 44+ endpoints:

| Test Class | Endpoints | Validation |
|-----------|-----------|-----------|
| TestAPIEndpoints | 4 | root, health, docs, OpenAPI schema |
| TestArmEndpoints | 16 | status, position, pose, joint/cartesian/all moves, stop, estop, origin, gripper, workspace |
| TestVisionEndpoints | 8 | status, color detection, apriltag, classify, inspect, threshold |
| TestTaskEndpoints | 10 | full lifecycle (create, list, get, start, pause, resume, progress, cancel, delete) |
| TestMonitorEndpoints | 6 | system status, sensors, safety, logs, statistics, health |

End-to-end workflow tests: `test_sampling_workflow`, `test_vision_motion_pipeline`, `test_safety_integration`.

#### 3.3 Loop Engineering Pipeline Tests (`loop_engineering/tests/test_integration.py`)
- Full pipeline integration: `generate_all() → evaluate()` with 7-dimensional evaluator
- Skill Extractor: trace pattern extraction, effectiveness tracking, persistence
- Knowledge Inheritor: version lineage, auto-inheritance, deprecation, core memory management
- Meta-Optimizer: strategy evolution, convergence detection, template generation
- Loop Runner: full setup, training callback, disabled mode, result serialization
- Performance benchmarks: evaluator < 100ms (100 tasks), context manager < 200ms (500 updates), skill extractor < 500ms (50 traces × 10 actions)

#### 3.4 Comprehensive Test Runner (`run_all_tests.py`)
- Four suites: blackbox, whitebox, performance, security
- Subprocess execution with 300s timeout per suite
- Regex-based pytest output parsing
- JSON report with summary statistics, per-suite breakdown, auto-generated recommendations
- CLI flags: `--quick`, `--verbose`, `--json`, `--report`, `--suites`

---

### 4. Docker Containerized Deployment

**Files**: `rpi_control/Dockerfile`, `rpi_control/docker-compose.yml`

#### 4.1 Multi-Stage Docker Build
- **Builder stage**: Python 3.11-slim-bookworm, gcc/g++/make, atlas, openblas, pip install to `/install`
- **Production stage**: Runtime deps only (libatlas, libopenblas, libgomp1, curl, i2c-tools, libgpiod2), non-root `sampling` user, app code in `/app/rpi_control/`
- Exposes ports 8000 (REST API) and 8001 (WebSocket)
- Health check: `curl -f http://localhost:8000/api/health`
- Multi-arch: `linux/amd64` and `linux/arm64` via `docker buildx`

#### 4.2 Docker Compose Four-Service Orchestration

| Service | Activation | Function |
|---------|-----------|----------|
| rpi-control | Always on | REST API + WebSocket, mounts hardware devices, resource limits 1.5 CPU / 512MB memory |
| cloud-sync | Profile `cloud` | 60s async sync loop, depends on rpi-control healthy |
| model-trainer | Profile `training` | Single training round, ephemeral |
| loop-trainer | Profile `loop`/`all` | Loop engineering with 10 iterations, ephemeral |

---

### 5. WebSocket Real-Time Telemetry

**Files**: `rpi_control/web/websocket/telemetry.py`, `rpi_control/web/websocket/handler.py`

- **WebSocketManager**: Max 100 concurrent clients, UUID-based IDs, `asyncio.Lock` thread safety, `broadcast()` with auto-removal of dead clients
- **Production Telemetry**: 10 Hz push rate, four data sources:

| Data Source | Content |
|------------|---------|
| Arm State | Joint positions, velocities, end-effector pose, is_moving, is_homed, gripper state/force |
| Safety Agent | Emergency stop flag, safety level, warnings, joint limits, workspace status, collision risk |
| Sensor Data | Temperature, humidity, voltage, current, distance |
| System Status | CPU usage, memory usage, uptime, connected clients |

Each message includes sequence number and timestamp. Event-driven: `send_status_change()`, `send_error()`.

---

### 6. Hardware Simulation / Real Dual Mode

All hardware interfaces implement simulation/real dual mode:
- **STM32**: Auto simulation when `pyserial` unavailable, commands logged as `SIM TX`, returns `#SIM:OK`
- **OpenMV**: Falls back to simulation on connection failure
- **Test fixtures**: `_disable_hardware` autouse fixture monkeypatches `HAS_PYSERIAL = False`
- **Hardware test markers**: `@pytest.mark.hardware` tests skipped by default, run only with `--hardware` flag

This enables 90%+ development and testing without physical hardware.

---

### 7. Fault Isolation & Tolerance

- **Agent fault isolation**: Each agent initialized independently; critical failures log ERROR, non-critical WARNING
- **Error Notifier** (`utils/error_handler.py`): Singleton collecting runtime errors, `asyncio.Lock` protected, `deque(maxlen=100)` O(1) truncation, retry decorator with configurable max retries and backoff
- **Config Loader**: YAML + environment variable overrides, deep merge, boolean/number parsing

---

### 8. Firmware Verification & System Diagnostics

**Files**: `verify_firmware.py`, `rpi_control/scripts/verify_system.py`

**STM32 Firmware Verification**: Auto-scans serial ports, probes with `#SYS:INFO!`, runs 13 servo protocol tests, generates JSON report with pass/fail details and timing.

**System Verification**: 11 sections covering agent imports, orchestrator initialization, model files, training reports, Docker/RPi configuration, config files, training data quality, hardware interfaces, performance benchmarks, RPi health, and cross-validation readiness.

---

## Part 2: Core Algorithm Detailed Explanation

---

### 1. Kinematics Algorithms

**File**: `rpi_control/motion/kinematics.py`

#### 1.1 DH Parameter Table

Standard DH convention with 6 joints:

| Joint | a (mm) | α (deg) | d (mm) | θ_offset (deg) | Description |
|-------|--------|---------|--------|----------------|-------------|
| 0 | 0 | 90 | 85 | 0 | Base rotation |
| 1 | 120 | 0 | 0 | -90 | Shoulder |
| 2 | 110 | 0 | 0 | 0 | Elbow |
| 3 | 0 | 90 | 0 | 0 | Wrist pitch |
| 4 | 0 | -90 | 95 | 0 | Wrist roll |
| 5 | 0 | 0 | 65 | 0 | Gripper |

#### 1.2 Forward Kinematics — Transformation Chain

```
T_i = [
  [cos(θ_eff),  -sin(θ_eff)·cos(α),   sin(θ_eff)·sin(α),  a·cos(θ_eff)],
  [sin(θ_eff),   cos(θ_eff)·cos(α),  -cos(θ_eff)·sin(α),  a·sin(θ_eff)],
  [0,            sin(α),               cos(α),              d           ],
  [0,            0,                    0,                   1           ]
]
```

`forward_kinematics()` iteratively multiplies: `T = I @ T₀ @ T₁ @ ... @ T₅`. Returns 4×4 pose and 6 intermediate transforms.

Euler angle extraction using ZYX convention with gimbal lock handling.

#### 1.3 Inverse Kinematics — Pieper's Decoupling Method

**Step 1**: Euler → rotation matrix: `R = R_z(yaw) · R_y(pitch) · R_x(roll)`

**Step 2**: Wrist center: `W = [x,y,z] - d₆ · R[:,2]`

**Step 3**: Position IK (joints 0-2):
- θ₁: `atan2(y, x)` and `atan2(y, x) + π` (antipodal)
- θ₃: Cosine law `cos(θ₃) = (r² + z'² - a₁² - a₂²) / (2·a₁·a₂)`, two solutions (elbow up/down)
- θ₂: `atan2(z', r) - atan2(k₂, k₁)`

**Step 4**: Orientation IK (joints 3-5):
- θ₅: `acos(R₃₆[2,2])`, two solutions (wrist flip)
- θ₄, θ₆: Standard spherical wrist formulas with singularity handling

**Step 5**: Filter (joint limits) and sort (minimum joint displacement)

**Complexity**: Up to 8 solutions, O(1) per solution.

#### 1.4 Jacobian — Analytical Geometric

For each joint i: `J_v[:,i] = z_i × (p_end - p_i)`, `J_ω[:,i] = z_i`

#### 1.5 PWM Conversion

```
angle → pwm: pwm = round(1500 + angle × 2000/π), clamped to [500, 2500]
pwm → angle: angle = (pwm - 1500) × π/2000
```

---

### 2. Trajectory Planning Algorithms

**File**: `rpi_control/motion/trajectory.py`

#### 2.1 Quintic Polynomial Interpolation

```
blend(s) = 10s³ - 15s⁴ + 6s⁵
Boundary conditions: position, velocity, acceleration all zero at endpoints
→ C² continuity guaranteed
Complexity: O(n), n = duration/dt
```

#### 2.2 S-Curve Velocity Profile

7-phase profile with bounded jerk:
- Phase 1-3: Acceleration (jerk +J_max, 0, -J_max)
- Phase 4: Constant velocity
- Phase 5-7: Deceleration (jerk -J_max, 0, +J_max)

Phase timing computed from max_vel, max_accel, max_jerk with automatic triangular fallback when distance is insufficient.

Numerical integration (Euler) with clamping.

#### 2.3 Trapezoidal Velocity Profile

3-phase profile with closed-form analytical position formulas.

#### 2.4 Path Smoothing

Iterative Laplacian smoothing: `smoothed[i] = (1-α)·p[i] + (α/2)·p[i-1] + (α/2)·p[i+1]`

#### 2.5 Cartesian Linear Path

Linear position interpolation + Euler-angle orientation with shortest-path wrapping.

---

### 3. Collision Detection Algorithms

**File**: `rpi_control/motion/collision.py`

#### 3.1 AABB Intersection

Standard axis-aligned bounding box overlap test: O(1).

#### 3.2 Self-Collision Detection

- Arm modeled as 5 cylindrical links (radius = 25.0 mm)
- Adjacent links excluded
- Minimum segment-to-segment distance computation (parametric solution with clamping)
- Threshold = sum of radii + safety_margin (default 60 mm)
- Complexity: O(1) for 5-link arm

#### 3.3 Environment Collision Detection

Two-phase algorithm:
1. **Broad phase**: AABB overlap test
2. **Narrow phase**: Point-to-segment distance

#### 3.4 Safe Retreat Path

Linear interpolation with collision checking per step. Fallback: raise shoulder joint by 0.2 rad if blocked.

---

### 4. Workspace Analysis Algorithms

**File**: `rpi_control/motion/workspace.py`

#### 4.1 Monte Carlo Boundary

Random joint sampling within limits → FK → point cloud. Complexity: O(num_samples × 6).

#### 4.2 Reachability

IK-based position-only check. Complexity: O(1).

#### 4.3 Dexterity

Yoshikawa manipulability: `w = √(det(J·Jᵀ))`. Higher w = better dexterity, w = 0 at singularities.

#### 4.4 Sampling Grid

Rectangular or hexagonal pattern generation with reachability filtering.

#### 4.5 TSP Order Optimization

Greedy nearest-neighbor heuristic. Complexity: O(n²).

---

### 5. Hybrid Force/Position Control Algorithms

**File**: `rpi_control/motion/force_control.py`

#### 5.1 Impedance Control

```
M·ẍ + D·(ẋ - ẋ_d) + K·(x - x_d) = F_ext
```
Solved for acceleration, second-order integration with 1ms control cycle.

Directional stiffness: `K_eff = K_base - 0.8·k·(d·dᵀ)` (80% reduction in specified direction).

#### 5.2 Admittance Control

```
M_d·ẍ + D_d·ẋ = F_ext - F_d
```
No stiffness term — damped mass responding to force error. Better for high-stiffness environments.

#### 5.3 Hybrid Force/Position Control

```
τ = S·τ_pos + (I - S)·τ_force
```
where S is a 6×6 diagonal selection matrix.

#### 5.4 Compliant Grasping

6-state machine: APPROACH → CONTACT → GRASPING → GRASPED → RELEASING → RELEASED (+ FAILED).

State transitions:
- APPROACH → CONTACT: force > 1.5N
- CONTACT → GRASPING: immediate
- GRASPING → GRASPED: force > 3.0N
- GRASPED → FAILED: slip detection triggers
- RELEASING → RELEASED: force < 0.1N AND gripper open > 10mm

Supports parallel gripper (adaptive grip force) and suction cup (vacuum force calculation).

#### 5.5 Slip Detection

Dual-condition: `force_drop > 0.3N AND std_increase > 0.1`

Rationale: slipping objects show decreased mean force and increased variance.

#### 5.6 Peg-in-Hole Search

Archimedean spiral: `r(t) = radius_max · t / (num_turns·2π)`, expanding from center. Parameters: 5mm radius, 0.5mm pitch, 3 turns.

#### 5.7 End-Effector Calibration

SVD-based rigid transformation (Arun's method) from ≥ 3 point pairs.

---

### 6. Hard Real-Time Safety Control Algorithms

**File**: `rpi_control/safety/realtime_safety.py`

#### 6.1 Control Cycle

- Control loop: **1 ms (1000 Hz)**
- Fault response deadline: **< 10 ms**
- Safety history: 1000 entries

#### 6.2 Safety State Machine

```
NORMAL ↔ REDUCED_SPEED ↔ PROTECTIVE_STOP ↔ SAFEGUARD_STOP ↔ EMERGENCY_STOP
                                                                     ↕
                                                                  FAULT
                                                                     ↕
                                                                 RECOVERY
```

#### 6.3 Per-Cycle 10-Step Safety Check

Each 1ms cycle:
1. Update joint states (position, velocity, torque)
2. Update TCP velocity
3. Check joint limits → JOINT_LIMIT_VIOLATION
4. Check speed limits (joint > 5.0 rad/s → SPEED_LIMIT_EXCEEDED; TCP > 1000 mm/s → critical)
5. Collision detection (distance-based: < 300mm warning, < 100mm protective stop; torque-based: total > 10 Nm)
6. Torque limit check (single joint > 50 Nm → TORQUE_OVERLOAD)
7. ISO/TS 15066 force limit (contact force > 65N face limit → FORCE_LIMIT_EXCEEDED)
8. Communication check (heartbeat timeout > 100ms → COMMUNICATION_TIMEOUT)
9. Clock sync check (drift > 100 ppm → CLOCK_DRIFT)
10. Update safety state

#### 6.4 ISO/TS 15066:2016 Compliance

Collaborative force limits for 11 body regions, safety speed limits (reduced: 250 mm/s, monitored: 2000 mm/s, TCP max: 1000 mm/s, joint max: 5.0 rad/s), safety distances (separation: 200mm, protective stop: 100mm, reduced speed zone: 500mm, collision warning: 300mm).

#### 6.5 Network Redundancy & Clock Sync

- Primary/backup dual network monitoring
- Both networks lost → critical communication timeout
- PTP (IEEE 1588) and NTP clock synchronization
- Drift detection: > 100 ppm triggers warning

#### 6.6 Safe Homing

Smoothstep interpolation: `s = 3t² - 2t³`, 50 steps, max 0.05 rad per step.

---

### 7. Loop Engineering Framework

**Files**: `rpi_control/loop_engineering/`, `rpi_control/config/settings.yaml`

#### 7.1 Overview

Automated closed-loop optimization: propose change → execute → evaluate → retain best.

#### 7.2 Seven Components

| Component | Function | Key Parameters |
|-----------|----------|---------------|
| **Profiler** | Real-time slow operation detection, bottleneck identification | Threshold 100ms, Top N report |
| **Interaction Tracker** | Redundant call detection, context bloat monitoring | Redundancy threshold 3, context warning 20 messages |
| **Evaluator (7-Dim)** | Quantifies system performance across 7 dimensions | Latency 15%, Reliability 20%, Quality 15%, Efficiency 15%, Robustness 15%, Context Health 10%, Reusability 10% |
| **Context Manager** | Auto-compresses oversized contexts, prevents state decay | Max history 100, compression threshold 50 messages |
| **Skill Extractor** | Mines high-frequency reuse patterns into reusable skills | Min reuse 2, similarity threshold 0.7 |
| **Knowledge Inheritor** | Cross-version knowledge transfer, decay-based pruning | Core memory retention 80%, decay threshold 0.3 |
| **Meta-Optimizer** | Evolutionary strategy optimization | Min improvement 0.05, strategy history 50 |

#### 7.3 Loop Runner

- Max iterations: 10
- Convergence threshold: consecutive improvement < 0.01
- Convergence patience: 3
- Min loop interval: 1.0s
- Callbacks: `on_iteration`, `on_convergence`

---

> **Document Version**: v1.0 | **Generated**: 2026-08-03 | **Authors**: Intelligent Sampling Robotic Arm Team