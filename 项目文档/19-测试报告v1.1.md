# 测试报告 v1.1

| 属性 | 内容 |
|------|------|
| 文档版本 | v1.1.0 |
| 测试日期 | 2026-08-15 |
| 被测版本 | v1.1（基线 v1.0） |
| 测试环境 | Windows（仿真模式，无硬件） |
| 关联文档 | [08-验证报告.md](08-验证报告.md)、[18-更新报告v1.1.md](18-更新报告v1.1.md) |

---

## 1. 结论

**v1.1 全量回归测试通过。** 后端 Python 测试 **412 passed / 11 skipped / 0 failed**；App 侧 `flutter analyze` 0 error、`flutter test` UI 冒烟测试全部通过。无阻断项，具备发布条件。

---

## 2. 后端全量测试

### 2.1 执行结果（实测）

```bash
python -m pytest rpi_control/tests -q
```

```
412 passed, 11 skipped, 0 failed, 1 warning in 254.28s (0:04:14)
```

> 跳过项（11）为需硬件 / 串口 / 特定环境的用例，非失败；1 warning 为 starlette.testclient 的弃用提示，不影响结果。

### 2.2 测试分类明细

| 分类 | 覆盖 | 结果 |
|------|------|------|
| 运动学 / 轨迹规划 | 正逆解、轨迹生成、工作空间 | ✅ 通过 |
| 力控 / 抓取 / 坐标定位 | 阻抗控制、轴孔装配、像素→机器人坐标 | ✅ 通过 |
| 多智能体 Agent | 编排 + 约束护栏（11 项护栏测试） | ✅ 通过 |
| API 集成 / 黑盒 / 白盒 | REST 接口、鉴权适配、核心逻辑 | ✅ 通过 |
| 多端互通 | 鉴权 / 设备中心 / WiFi API / WebSocket 中枢 / ESP32 超时回归 | ✅ 通过 |
| 安全分析 | 威胁模型、安全审计 | ✅ 通过 |
| 数据库 CRUD | 用户 / 设备 / WiFi / 样本 / 配置 / 闭环工程 6 模型 增删改查回归 | ✅ 通过 |
| 性能 / 压力 | 性能基准、极端工况 | ✅ 通过或按环境跳过 |

---

## 3. App 侧验证

### 3.1 静态分析

```bash
flutter analyze
```

- 结果：**0 error**（可能存在的 info 级提示不影响构建）

### 3.2 UI 冒烟测试（`mobile_app/test/smoke_test.dart`）

| 用例 | 验证点 | 结果 |
|------|--------|------|
| 登录页渲染 | 标题「智能采样机械臂」、登录按钮 | ✅ |
| 控制页渲染 | 6 关节滑杆、急停按钮（滚动后可见） | ✅ |
| 监控页渲染 | 「实时监控」「多端中枢」 | ✅ |
| WiFi 页渲染 | 「WiFi 配网」「连接热点 (STA)」 | ✅ |
| 设置页渲染 | 「设置」「退出登录」 | ✅ |
| 通用组件渲染 | SectionHeader / StatusCard / JointSlider | ✅ |

---

## 4. v1.1 重点回归项

| 回归项 | 说明 | 结果 |
|--------|------|------|
| 机械臂状态持久化 | 重启后经 `ConfigRepository` 恢复 `arm_status` | ✅ |
| 视觉阈值持久化 | `vision_thresholds` 保存 / 恢复 | ✅ |
| 系统配置 CRUD | 单项读写 / 删除（恢复默认）/ 备份恢复 | ✅ |
| 闭环工程 6 模型 CRUD | 评估报告 / 性能画像 / 交互日志 / 技能 / 知识血缘 / 元技能 | ✅ |
| 样本 CRUD | 样本创建 / 查询 / 列表 / 更新 / 删除 | ✅ |
| 鉴权回归 | 变更类接口携带 Bearer Token 访问 | ✅ |

---

## 5. 环境信息

| 项 | 值 |
|----|----|
| Python | 3.14 |
| pytest | rpi_control/tests 全量 |
| FastAPI TestClient | 仿真模式 |
| Flutter | 本地 SDK（`.flutter_sdk/`） |
| 数据库 | SQLite（`data/sampling.db`） |
