# 测试报告 v1.2

| 属性 | 内容 |
|------|------|
| 文档版本 | v1.2.0 |
| 测试日期 | 2026-08-19 |
| 被测版本 | v1.2（基线 v1.1） |
| 测试环境 | Windows（仿真模式，无硬件） |
| 关联文档 | [08-验证报告.md](08-验证报告.md)、[20-更新报告v1.2.md](20-更新报告v1.2.md) |

---

## 1. 结论

**v1.2 全量回归测试通过。** 后端 Python 测试 **412 passed / 11 skipped / 0 failed**（基线），另新增 **19 项帧协议正式回归测试全部通过**（合计 **431 passed / 11 skipped / 0 failed**）；四个 ML 模型独立精度校验全部 PASS；v1.2 专项验证（统一帧协议编解码 / CRC16 篡改丢弃 / 流式解析 / STM32 帧模式仿真接入）全部通过。无阻断项，具备发布条件。

---

## 2. 后端全量测试

### 2.1 执行结果（实测）

```bash
python -u -m pytest rpi_control/tests -v
```

```
445 passed, 11 skipped, 0 failed, 1 warning in 326.26s (0:05:26)
```

> 跳过项（11）为需硬件 / 串口 / 特定环境的用例，非失败；1 warning 为 starlette.testclient 的弃用提示，不影响结果。

### 2.2 测试分类明细

| 分类 | 覆盖 | 结果 |
|------|------|------|
| 运动学 / 轨迹规划 | 正逆解、轨迹生成、工作空间 | ✅ 通过 |
| 力控 / 抓取 / 坐标定位 | 阻抗控制、轴孔装配、像素→机器人坐标 | ✅ 通过 |
| 多智能体 Agent | 编排 + 约束护栏（11 项护栏测试） | ✅ 通过 |
| API 集成 / 黑盒 / 白盒 | REST 接口、鉴权适配、核心逻辑 | ✅ 通过 |
| 多端互通 | 鉴权 / 设备中心 / WiFi API / WebSocket 中枢 / ESP32 超时回归 | ✅ 通过 |
| 安全分析 | 威胁模型、安全审计 | ✅ 通过 |
| 数据库 CRUD | 用户 / 设备 / WiFi / 样本 / 配置 / 闭环工程 6 模型 增删改查回归 | ✅ 通过 |
| 性能 / 压力 | 性能基准、极端工况 | ✅ 通过或按环境跳过 |
| **统一帧协议（新增）** | **CRC16 / 编解码 / 篡改丢弃 / 流式解析 / STM32 帧模式** | ✅ **19/19 通过** |

---

## 3. v1.2 专项验证

### 3.1 统一帧协议（`test_frame_protocol.py`，19 项）

| 用例 | 验证点 | 结果 |
|------|--------|------|
| 编解码往返 | move_joint / move_all / estop / get_status 帧 encode→decode 一致 | ✅ |
| CRC16 篡改检测 | 翻转载荷 / CRC 字节 → 坏帧丢弃 | ✅ |
| 长度字段不符 | 伪造长度字段 → 坏帧丢弃 | ✅ |
| 流式解析 | 杂散字节容错、双帧粘连、半帧保留待续 | ✅ |
| STM32 帧模式 | `set_frame_mode` / `send_frame` / `frame_move_joint` / `frame_emergency_stop` 仿真接入 | ✅ |
| ServoController 集成 | 帧模式下运动 / 单关节 / 回零 / 急停 / 软停 | ✅ |

### 3.2 模型独立精度校验（`python -m training.test_models`）

| # | 模型 / 模块 | 指标 | 结果 |
|---|------------|------|------|
| 1 | Vision 坐标变换 | round-trip 像素一致性 6666/6666（max_err=0.00px），工作空间内 6666/6666 | ✅ PASS |
| 2 | Motion IK（热启动 + 解析 IK） | 最终误差 mean=0.21mm / p90=0.41mm / max=0.50mm | ✅ PASS |
| 3 | Safety | Acc=0.9587, Prec=0.9577, Rec=0.9891, F1=0.9731（111129 样本） | ✅ PASS |
| 4 | Quality | R²=0.9916, MAE=2.55, RMSE=3.16（102516 样本） | ✅ PASS |
| 5 | Collision | Acc=0.9702, Prec=0.4590, Rec=0.9921（45000 样本, 碰撞率 2.53%） | ✅ PASS |

### 3.3 v1.2 增强特性（`test_v12_enhancements.py`，14 项）

| 用例 | 验证点 | 结果 |
|------|--------|------|
| 传感器过载监控 | 正常温度/电流无过载事件；温度 >70°C → TEMPERATURE_OVERLOAD；电流 >3A → CURRENT_OVERLOAD；传感器值跨周期保持 | ✅ 4/4 |
| Vision EMA 滤波 | 首帧初始化 EMA；中值+EMA 抑制单帧抖动；EMA 递推公式（α=0.5 → 100→120 得 110）；历史中断自动重置；`reset_ema` | ✅ 4/4 |
| Sampling 边界增强 | 4×4 分层 + 边界增强共 32 点；全部在工作空间内；边界点 16 个且 priority=1；无重复坐标；四角必被覆盖 | ✅ 6/6 |

---

## 4. v1.2 重点回归项

| 回归项 | 说明 | 结果 |
|--------|------|------|
| 统一帧协议启用 | `settings.yaml protocol.frame_enabled=true` 经 `main.py` 正确注入 STM32 帧模式 | ✅ |
| 遥测 20Hz | `TelemetryStream` 默认推送率 20Hz，`set_push_rate` 可配置覆盖 | ✅ |
| 传感器过载监控 | `update_joint_sensors` → `_check_sensor_overload` → 温度/电流过载事件 → PROTECTIVE_STOP | ✅ |
| Vision EMA 滤波 | `get_smoothed_position` 多帧融合、历史中断自动重置 | ✅ |
| Sampling 分层边界增强 | 等距 + 边界增强、`seen` 集合去重角点 | ✅ |
| 版本号 | settings.yaml / server.py / main.py / README 统一 1.2.0 | ✅ |
| 数据库 CRUD / 多端互通 / App | v1.1 能力在 v1.2 全量回归中保持通过 | ✅ |

---

## 5. 环境信息

| 项 | 值 |
|----|----|
| Python | 3.14.3 |
| pytest | 9.1.1（rpi_control/tests 全量 456 项：445 passed + 11 skipped，含帧协议 19 项与 v1.2 增强 14 项） |
| FastAPI TestClient | 仿真模式 |
| 数据库 | SQLite（`data/sampling.db`） |
| 模型 | motion_ik / safety / quality / collision（Round 12 重训后校验） |
| 测试时长 | 全量回归 5 分 26 秒 + 专项验证 0.2 秒 |
